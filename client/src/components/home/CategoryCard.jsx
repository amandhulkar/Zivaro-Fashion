import { motion } from 'framer-motion'
import { FiArrowUpRight } from 'react-icons/fi'
import { Link } from 'react-router-dom'

const CategoryCard = ({ category }) => <motion.article whileHover={{y:-8}} transition={{duration:.25}}><Link to={`/shop?category=${category.slug}`} className={`group relative block overflow-hidden rounded-3xl bg-gradient-to-br ${category.tone} p-1 shadow-soft`}>{category.image && <img src={category.image} alt={category.title} className="absolute inset-0 h-full w-full object-cover transition duration-500 group-hover:scale-105" />}<div className={`relative flex min-h-72 flex-col justify-end rounded-[1.35rem] border border-white/70 p-7 ${category.image ? 'bg-gradient-to-t from-primary/85 via-primary/25 to-transparent' : 'bg-white/55 backdrop-blur'}`}><p className={`caption ${category.image ? 'text-white/80' : ''}`}>{category.count}</p><div className="mt-12 flex items-end justify-between gap-4"><div><h3 className={`text-3xl font-semibold ${category.image ? 'text-secondary' : 'text-primary'}`}>{category.title}</h3><p className={`mt-3 text-sm leading-6 ${category.image ? 'text-white/75' : 'text-muted'}`}>{category.subtitle}</p></div><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-secondary text-primary shadow-soft"><FiArrowUpRight /></span></div></div></Link></motion.article>
export default CategoryCard
